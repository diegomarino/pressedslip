/**
 * @fileoverview Bottom footer component placeholder.
 */
import type { ReactElement } from "react";

/**
 * Footer placeholder rendered at the bottom of every composition.
 * Currently an empty fixed-padding div.
 */
export function ShellBottom(): ReactElement {
  return <div style={{ width: "100%", padding: 4 }} />;
}
